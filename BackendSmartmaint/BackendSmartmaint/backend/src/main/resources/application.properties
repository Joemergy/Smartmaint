# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://localhost:5432/smartmaint
spring.datasource.username=smartmaintadmin
spring.datasource.password=Ralmergy77777+
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.open-in-view=false

# Mostrar SQL y parámetros en consola
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Server
server.port=8080

# SQL init
spring.sql.init.mode=never

# Logging general
logging.level.root=DEBUG
logging.level.org.springframework=DEBUG
logging.level.com.smartmaint=DEBUG

#Sobreescribir el bean duplicado
spring.main.allow-bean-definition-overriding=true

